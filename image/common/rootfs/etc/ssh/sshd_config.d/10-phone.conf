# SSH policy of the clean image: root never logs in over SSH, and an empty
# password is never accepted. Host keys are made on the first boot
# (first-boot-ssh-keys.service), so no two phones share them.
PermitRootLogin no
PermitEmptyPasswords no
MaxAuthTries 4
